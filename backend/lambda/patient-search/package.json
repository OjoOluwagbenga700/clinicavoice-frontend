{
  "name": "patient-search",
  "version": "1.0.0",
  "description": "Patient search Lambda function",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "aws-sdk-client-mock": "^3.0.0"
  }
}
