{
  "name": "clinicavoice-patients-lambda",
  "version": "1.0.0",
  "description": "Patient management Lambda function for ClinicaVoice",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "test": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@aws-sdk/client-lambda": "^3.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.15"
  }
}
